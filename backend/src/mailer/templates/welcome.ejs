<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to SendIT - Your Account is Ready!</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background-color: #f6f8fa;
            color: #333;
            line-height: 1.6;
        }
        
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .logo {
            width: 120px;
            height: auto;
            margin-bottom: 20px;
        }
        
        .main-heading {
            font-size: 24px;
            font-weight: 600;
            color: #333;
            margin-bottom: 10px;
        }
        
        .content-card {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            padding: 40px;
            margin-bottom: 30px;
        }
        
        .user-avatar {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            margin: 0 auto 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #DBBB02;
            color: #fff;
            font-size: 32px;
            font-weight: bold;
        }
        
        .card-heading {
            font-size: 20px;
            font-weight: 600;
            color: #333;
            text-align: center;
            margin-bottom: 15px;
        }
        
        .greeting {
            text-align: center;
            font-size: 16px;
            color: #333;
            margin-bottom: 25px;
        }
        
        .content-text {
            font-size: 14px;
            color: #333;
            margin-bottom: 15px;
            line-height: 1.6;
        }
        
        .highlight-text {
            font-weight: 600;
            text-align: center;
            margin: 20px 0;
            color: #DBBB02;
            font-size: 16px;
        }
        
        .cta-button {
            display: inline-block;
            background-color: #DBBB02;
            color: #fff;
            text-decoration: none;
            padding: 12px 24px;
            border-radius: 6px;
            font-weight: 600;
            font-size: 14px;
            text-align: center;
            margin: 20px 0;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        
        .cta-button:hover {
            background-color: #c4a902;
        }
        
        .support-link {
            text-align: center;
            margin: 20px 0;
        }
        
        .support-link a {
            color: #DBBB02;
            text-decoration: none;
            font-weight: 600;
        }
        
        .support-link a:hover {
            text-decoration: underline;
        }
        
        .footer {
            text-align: center;
            margin-top: 30px;
        }
        
        .footer a {
            color: #DBBB02;
            text-decoration: none;
            font-size: 14px;
        }
        
        .footer a:hover {
            text-decoration: underline;
        }
        
        .footer .separator-dot {
            color: #333;
            margin: 0 8px;
        }
        
        @media (max-width: 600px) {
            .container {
                padding: 10px;
            }
            
            .content-card {
                padding: 20px;
            }
            
            .main-heading {
                font-size: 20px;
            }
            
            .card-heading {
                font-size: 18px;
            }
            
            .logo {
                width: 100px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header Section -->
        <div class="header">
            <div class="logo">
                <!-- Replace this with your actual logo URL when hosting -->
                <div style="width: 120px; height: 60px; background-color: #DBBB02; border-radius: 8px; display: flex; align-items: center; justify-content: center; color: #fff; font-weight: bold; font-size: 18px; margin: 0 auto;">
                    SendIT
                </div>
            </div>
            <h1 class="main-heading">Welcome to SendIT!</h1>
        </div>

        <!-- Main Content Card -->
        <div class="content-card">
            <div class="user-avatar">
                <% if (typeof profilePicture !== 'undefined' && profilePicture) { %>
                    <img src="<%= profilePicture %>" alt="<%= name %>'s profile" style="width: 80px; height: 80px; border-radius: 50%; object-fit: cover;">
                <% } else { %>
                    <%= name.charAt(0).toUpperCase() %>
                <% } %>
            </div>
            
            <h2 class="card-heading">Welcome to SendIT!</h2>
            
            <div class="greeting">
                Greetings, <strong><%= name %></strong>
            </div>
            
            <div class="content-text">
                Thank you for joining SendIT! We're excited to have you on board and help you with all your delivery needs.
            </div>
            
            <div class="content-text">
                Your account has been successfully created and you can now start sending parcels with ease. Our platform offers secure, fast, and reliable delivery services across the country.
            </div>
            
            <div class="highlight-text">
                Get started by creating your first parcel delivery!
            </div>
            
            <div class="content-text">
                If you have any questions or need assistance, our support team is here to help you every step of the way.
            </div>
            
            <div style="text-align: center;">
                <a href="<%= baseUrl %>/dashboard" class="cta-button">
                    Start Sending
                </a>
            </div>
            
            <div class="support-link">
                Need help getting started? <a href="<%= baseUrl %>/help">Visit our help center</a> or <a href="<%= baseUrl %>/contact">contact our support team</a>.
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <a href="<%= baseUrl %>/help">Help Center</a>
            <span class="separator-dot">•</span>
            <a href="<%= baseUrl %>/contact">Contact us</a>
        </div>
    </div>
</body>
</html> 